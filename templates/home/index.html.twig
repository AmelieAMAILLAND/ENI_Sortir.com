{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    <h1 class="mt-4 text-3xl text-center font-semibold">Sortir.com</h1>

    <p class="text-center mt-4 text-xl"> Bienvenue {{ app.user.pseudo ?? '' }} !</p>

    <div>
        <a href="{{ path('app_event_index') }}"
           class="block max-w-fit mx-auto mt-4 bg-amber-500 rounded-md py-2 px-3 text-white shadow-md hover:opacity-80">
            VOIR LES SORTIES
        </a>
    </div>

{% endblock %}
