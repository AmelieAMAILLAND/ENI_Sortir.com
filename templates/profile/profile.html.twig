{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block body %}
    <div class="flex items-center justify-center min-h-[90vh] mt-[-20px]">
        <div class="container mx-auto flex flex-col items-center justify-center">
            <div class="flex items-center justify-center mb-4">
                <img id="profile-picture-preview" src="{{ asset(user.getProfilePictureOrDefault()) }}"
                     data-default-src="{{ asset('images/profile/ajouter_des_photos.png') }}"
                     alt="Ma photo" class="w-[250px] h-[250px]">
            </div>

            <div class="w-full max-w-[600px] flex flex-col items-center">
                <div class="py-2 w-full flex flex-col justify-center items-center">
                    <h1 class="text-center text-2xl font-semibold mb-2">{{ user.pseudo }}</h1>
                    <!-- Agrandir encore plus l'encadré des informations, et ajouter du padding -->
                    <div class="bg-amber-50 rounded-md p-6 shadow-md w-full text-lg">
                        <p class="flex justify-between gap-3 mb-4 px-4"><span class="font-semibold">Prénom : </span>{{ user.firstName }}</p>
                        <p class="flex justify-between gap-3 mb-4 px-4"><span class="font-semibold">Nom : </span>{{ user.lastName }}</p>
                        <p class="flex justify-between gap-3 mb-4 px-4"><span class="font-semibold">Téléphone : </span>{{ user.phone }}</p>
                        <p class="flex justify-between gap-3 mb-4 px-4"><span class="font-semibold">Email : </span>{{ user.email }}</p>
                        <p class="flex justify-between gap-3 mb-4 px-4"><span class="font-semibold">Site de rattachement : </span>{{ user.site.name }}</p>
                    </div>

                    {% if app.user and app.user.id == user.id %}
                        <div class="mt-4 inline-flex space-x-4">
                            <a href="{{ path('app_modifierProfile') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                                Modifier mon profil
                            </a>
                            <a href="{{ path('app_event_index') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
                                Retour
                            </a>
                        </div>
                    {% else %}
                        <div class="mt-4">
                            <a href="{{ path('app_event_index') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
                                Retour
                            </a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
