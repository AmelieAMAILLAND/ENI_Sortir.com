{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
    <div class="relative w-full flex items-center justify-center">
        <div class="container mx-auto px-4 py-16 z-10">
            <div class="bg-white rounded-lg shadow-md p-8 max-w-xl mx-auto border-4 border-[#8800ff] text-center">
                <p class="font-bold mb-4">
                    Si un compte correspondant à votre email existe, un email contenant un lien pour réinitialiser votre mot de passe a été envoyé.
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p class="font-bold">
                    Si vous ne recevez pas d'email, vérifiez votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}" class="text-[#8800ff] hover:text-[#6600cc] underline">renvoyer un lien</a>.
                </p>
            </div>
        </div>
    </div>
{% endblock %}
